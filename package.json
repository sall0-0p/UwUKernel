{
  "name": "uwu-monorepo",
  "description": "Monorepo containing UwUntuCC related packages.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@uwu/kernel",
    "watch": "nodemon --watch packages/kernel --watch packages/syslib --watch startup.lua --watch test.lua --ext ts,lua --ignore \"packages/kernel/out\" --exec \"./deploy.sh\"",
    "deploy": "./deploy.sh"
  },
  "author": "_lordBucket"
}
